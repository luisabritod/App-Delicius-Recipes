import 'package:flutter/material.dart';

class SearchTabState extends StatelessWidget {
  const SearchTabState({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
